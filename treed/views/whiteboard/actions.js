// @flow

import type {ViewActionConfig} from '../../types'

const actions: ViewActionConfig = {
  edit: {
    shortcuts: {
      normal: 'enter',
    },
    alias: 'edit',
  },
  // ???
}

export default actions

